#define NUM LETTERS IN ALPHABET 26 
#define NEWLINE '\n'
const int minPasswordLength = 8;
for (int i = 1; i <= NUM LETTERS IN ALPHABET; i++) {
    //...
}
if (name[0] == NEWLINE) { 
    //...
}
if (strlen(passwd) < minPasswordLength) {
    //...
}
