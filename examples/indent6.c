struct Parameters param1 = { .initial = NULL,
    .end = NULL,
    .runningCount = 0};
struct LongParameters param2 = { .initial = NULL,
    .end = NULL,
    .runningCount = 0,
    .fileName = NULL,
    .limit = 0,
    .upper = false,
    .lower = true,
    .length = 0,
    .userName = NULL };
