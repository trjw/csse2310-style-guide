// When declaring pointers, the * should be next to the type, with a space
// before the variable/argument name:
int fn(int* num, char* str);
char* abc;
// However, if multiple pointers are declared in one statement, then the *
// should be next to the variable name:
char *str1, c, *str2, *str3;
