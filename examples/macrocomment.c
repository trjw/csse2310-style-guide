/// Macro to return the smallest of two numerical values (a and b)
#define MINIMUM(a, b) (((a) < (b)) ? (a) : (b))

#define MAXIMUM(a, b) (((a) > (b)) ? (a) : (b))
///< Macro to return the largest of two numerical values (a and b)

#define COURSE_NUMBER 2310
