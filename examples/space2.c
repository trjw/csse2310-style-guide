// Note the spaces around * _ && || ^ & | > ? : in these examples
int y = m * x + c;
bool isValid = exist(filename) && (someCondition || otherCondition);
regValue = (a ^ b) & (c | d);
max = (a > b) ? a : b;
